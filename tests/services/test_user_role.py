# import pytest
# import services.user_role as service

def test_get_all_user_roles():
    # roles = service.get_all()
    # assert isinstance(roles, list)
    # # Assuming there might be roles in DB, but for test DB it could be empty
    pass

def test_get_one_user_role():
    # role = service.get_one("admin")
    # assert role is None or hasattr(role, 'name')  # None if not found, or UserRole object
    pass